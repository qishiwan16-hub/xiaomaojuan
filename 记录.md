## 2026-03-17 04:35（北京时间 UTC+8）

- 任务：TASK-20260317-01 / 酒馆设置新增局域网链接
- 改了什么：
  - 在 `xiaomaojuan.sh` 的脚本加载链末尾新增 `lib/tavern_setting_override.sh`，让酒馆设置相关的新增逻辑在 `router.sh` 之后生效。
  - 新增 `lib/tavern_setting_override.sh`，加入局域网 IPv4 识别、局域网链接状态展示、`listen: true` 写入和 `hostWhitelist` 局域网白名单写入逻辑。
  - 扩展酒馆设置总览、详情页和动作分发，新增 `12 · 局域网链接`，支持一键写入、手动修改局域网 IP 和改回自动识别。
- 修复了什么：
  - 修复了酒馆设置里没有局域网访问入口的问题，避免用户还要手动改 `config.yaml` 才能让同一局域网设备访问酒馆。
  - 修复了“安全修复”与局域网访问场景混淆的问题，安全修复页现在明确说明它是本机访问模式，需要局域网访问时应改用新的局域网链接入口。
  - 修复了安全状态提示在局域网白名单场景下容易误判为“未套用本机防护”的问题，状态文案会区分本机访问和局域网白名单。
- 更新了什么：
  - 更新了酒馆设置页的项目数量、说明文案和交互提示，使网络访问相关设置拆分成“安全修复”和“局域网链接”两套清晰流程。
  - 更新了局域网链接详情页展示内容，新增自动识别 IPv4、当前准备写入地址、白名单已写入地址和局域网访问链接展示。
  - 更新了脚本自检结果，本次新增文件和入口文件已通过 `bash -n` 语法检查。

## 2026-03-17 04:45（北京时间 UTC+8）

- 任务：TASK-20260317-02 / 启动到运行阶段判定改为监听行
- 改了什么：
  - 修改 `lib/launch.sh` 的启动判定链，把 `xmj_launch_wait_for_running` 从 `Go to`、入口探测和进程存活兜底，改成只根据后台是否出现 `SillyTavern is listening on ...` 进入运行中。
  - 新增启动日志清洗和监听地址解析逻辑，支持从带终端颜色控制符的后台输出里提取监听地址并反推访问链接。
  - 调整 `lib/render.sh` 的运行页文案，把“等到 Go to 后切进运行页”改成“等到监听成功行后切进运行页”。
- 修复了什么：
  - 修复了启动阶段过早进入“运行中”的问题，避免在只探测到入口可访问或仅凭进程存活时提前切页。
  - 修复了带颜色的原生后台输出可能影响监听地址识别的问题，日志解析时会先去掉 ANSI 控制序列再匹配监听行。
  - 修复了历史启动日志回看时入口链接优先级不准确的问题，现会优先从监听成功行提取访问地址。
- 更新了什么：
  - 更新了运行阶段起始行的回溯判定，兼容新的“监听行判定进入运行阶段”日志标记。
  - 更新了启动超时提示，现在会明确提示后台一直没出现 `SillyTavern is listening on` 这一行。
  - 更新了脚本自检结果，本次修改后的 `xiaomaojuan.sh`、`lib/launch.sh`、`lib/render.sh`、`lib/tavern_setting_override.sh` 已通过 `bash -n` 语法检查。

## 2026-03-17 15:09（北京时间 UTC+8）

- 任务：TASK-20260317-03 / 阅读并梳理 ST-foxium 脚本结构
- 改了什么：
  - 阅读了外部脚本 `C:\Users\祁时晚\Downloads\foxium.sh`，按启动检查、修复功能、优化功能和菜单循环四个层次梳理其实现结构。
  - 提炼了脚本中的主要功能项、对应函数、作用对象和实现手段，准备输出为“内容 -> 如何实现”的对照说明。
  - 补充识别了脚本中的关键实现约束，包括对 `sed/grep/awk` 的文本替换依赖、对特定目录结构和旧版文件位置的假设。
- 修复了什么：
  - 修复了当前项目任务追溯里缺少这次阅读分析记录的问题，确保外部脚本分析任务也有持久化记录。
  - 修复了对目标链接读取路径不完整的阻塞，改为直接读取用户提供的本地 `foxium.sh` 文件本体进行分析。
  - 修复了分析范围不清的问题，已把目标限定为脚本内实际菜单功能与落地实现，而不是泛泛介绍仓库名义功能。
- 更新了什么：
  - 更新了对 ST-foxium 的结构认知，确认它是单文件 Bash 菜单工具，核心通过直接改 ST 文件和运行 git/npm 命令实现功能。
  - 更新了功能清单认知，确认其主菜单为“修复功能”和“优化功能”两组，并带启动检查与自动备份注入能力。
  - 更新了后续输出依据，本次说明将以 `foxium.sh` 中的具体函数和代码段为准。

## 2026-03-17 15:15（北京时间 UTC+8）

- 任务：TASK-20260317-04 / 生成 Foxium 与当前项目功能对照表
- 改了什么：
  - 对照 `foxium.sh` 的菜单项，补充梳理了当前项目中对应的启动、酒馆设置、依赖整理、更新、版本切换、备份、卸载与重装入口。
  - 明确标记了“已有对应实现”“部分对应”“暂无对应”的功能边界，并准备输出为结构化对照表。
  - 补充整理了两边实现方式差异，重点区分了 Foxium 的单文件文本替换方式与当前项目的分模块流程式实现。
- 修复了什么：
  - 修复了仅有单边功能说明、缺少项目内对应落点的问题，现在可以直接看出每个 Foxium 项在当前项目中对应到哪一层实现。
  - 修复了备份、依赖安装和卸载重装这几类跨模块能力不易对齐的问题，已统一映射到当前项目的统一备份流和维护流。
  - 修复了对“可复用 / 不建议照搬”边界不清的问题，输出会明确哪些交互思路可借鉴，哪些硬编码做法不该搬进来。
- 更新了什么：
  - 更新了当前项目能力画像，确认其在备份、更新、版本切换、依赖修复和安全/局域网设置方面已经明显强于 Foxium。
  - 更新了功能缺口画像，确认当前项目仍缺少“强制删除 ST 扩展”“模型列表注入”“Gemini 3 媒体白名单补丁”这类外科式补丁入口。
  - 更新了本次对照说明的依据文件，当前项目主要参考 `lib/launch.sh`、`lib/router.sh`、`lib/tavern_setting_override.sh`、`lib/backup_override.sh`、`lib/maintenance_override.sh`、`lib/update.sh`、`lib/version_switch.sh`。

## 2026-03-17 15:35（北京时间 UTC+8）

- 任务：TASK-20260317-05 / 优化酒馆设置并为写入操作增加前置文件备份
- 改了什么：
  - 在 `lib/tavern_setting_override.sh` 新增酒馆设置专用备份辅助链，统一生成 `backups/tavern-setting-files/时间戳-操作名/` 目录，并按操作复制对应源文件快照。
  - 重写酒馆设置总览页文案，明确提示所有会改文件的设置都会先备份对应文件，再继续执行修改。
  - 将端口修改、安全修复、多用户登录、浏览器跳转、头像高清修复、聊天加载修复、美化卡死修复、文件聊天上限、运行内存、备份保留数、局域网链接写入等设置项全部接入统一前置备份流程。
- 修复了什么：
  - 修复了酒馆设置内多处直接改写 `config.yaml`、`settings.json`、`server-main.js` 或面板配置文件但没有操作前备份的问题。
  - 修复了不同酒馆设置入口在修改前保护措施不一致的问题，避免部分设置可回滚、部分设置不可回滚。
  - 修复了设置执行成功后看不到本次备份落点的问题，状态提示现在会附带备份目录与备份文件摘要。
- 更新了什么：
  - 更新了酒馆设置写入链的交互标准，统一为“先备份对应文件，再执行修改，再返回结果与备份位置”。
  - 更新了酒馆设置页的说明信息，使用户能直接看到这类操作的备份目录位置与保护范围。
  - 更新了本次修改的自检结果，`xiaomaojuan.sh`、`lib/tavern_setting_override.sh`、`lib/router.sh`、`lib/config.sh`、`lib/maintenance.sh` 已通过 `bash -n` 语法检查。

## 2026-03-17 15:43（北京时间 UTC+8）

- 任务：TASK-20260317-06 / 将局域网链接白名单改为局域网网段写入
- 改了什么：
  - 在 `lib/tavern_setting_override.sh` 新增局域网网段换算与匹配逻辑，根据当前私网 IPv4 自动换算 `10.0.0.0/8`、`192.168.0.0/16` 或 `172.16.0.0/12`。
  - 重写局域网链接的白名单生成函数、状态识别函数和详情页展示内容，使局域网链接功能按网段写入 `hostWhitelist`，而不是继续写单个 IPv4。
  - 同步重写局域网链接执行成功文案，让页面显示的“将写入白名单”“已写入白名单”和实际写入到 `config.yaml` 的内容保持一致。
- 修复了什么：
  - 修复了局域网链接功能仍把单个局域网 IP 写进 `hostWhitelist`，与目标教程要求的 `192.168.0.0/16` 这类网段白名单不一致的问题。
  - 修复了局域网链接状态页和安全状态页仍按“单个 IP 白名单”判断的误报问题，避免写入网段后界面显示异常。
  - 修复了局域网 IP 输入页说明与实际行为不一致的问题，现在会明确说明输入的是本机 IPv4，最终写入的是对应局域网网段。
- 更新了什么：
  - 更新了局域网链接详情页说明，明确 `192.168.x.x` 会写成 `192.168.0.0/16`，其他私网段也会自动换算。
  - 更新了局域网链接状态文案，当前状态会带上可访问链接和对应白名单范围。
  - 更新了本次修改的自检结果，`xiaomaojuan.sh` 与 `lib/tavern_setting_override.sh` 已通过 `bash -n` 语法检查。

## 2026-03-17 16:00（北京时间 UTC+8）

- 任务：TASK-20260317-07 / 将酒馆默认文件夹设置提升为独立设置项并持久保存
- 改了什么：
  - 在 `lib/config.sh` 新增 `XMJ_TAVERN_SETTING_USER_NAME` 默认配置项，让酒馆设置里默认使用的 data 用户文件夹名可以持久保存到 `xiaomaojuan.conf`。
  - 在 `lib/tavern_setting_override.sh` 新增 `13 · 默认文件夹设置` 独立入口、状态显示、详情页和写入逻辑，并把写入前备份接入统一酒馆设置备份链。
  - 重写卡顿修复、聊天加载卡死修复、美化卡死修复三页的说明和动作区，移除原来挂在单项功能里的“修改用户名”入口，统一改为引用新的默认文件夹设置。
- 修复了什么：
  - 修复了默认文件夹只存在于单个修复项子流程里的问题，避免用户需要在多个功能页里分别找入口。
  - 修复了默认文件夹只保存在会话变量里、重新进入脚本后又回到 `default-user` 的问题，现在设置后会持续保留。
  - 修复了相关修复项继续使用“用户名”说法导致含义不清的问题，当前页面和提示已统一为“默认文件夹 / data 用户文件夹”。
- 更新了什么：
  - 更新了酒馆设置首页项目数与说明文案，明确需要改 `settings.json` 目标文件夹时统一走 `13 默认文件夹设置`。
  - 更新了相关修复项的交互规则，当前只保留执行入口；修改目标文件夹需要回到酒馆设置首页单独设置。
  - 更新了本次修改的自检结果，`xiaomaojuan.sh`、`lib/config.sh`、`lib/tavern_setting_override.sh` 已通过 `bash -n` 语法检查。

## 2026-03-17 16:26（北京时间 UTC+8）

- 任务：TASK-20260317-08 / 修复默认文件夹设置入口未显示并补充页内跳转
- 改了什么：
  - 修正了当前生效的酒馆设置总览页渲染内容，把 `13 · 默认文件夹设置` 真正显示到卡片区和操作区，并补充了“默认文件夹入口：13”提示行。
  - 在卡顿修复、聊天加载卡死修复、美化卡死修复三页新增 `2 前往默认文件夹设置` 跳转入口。
  - 调整默认文件夹设置页的返回链路，从相关功能页进入时可以回到来源页，而不是一律掉回酒馆设置首页。
- 修复了什么：
  - 修复了酒馆设置动作分发已经支持 `13`，但首页概览页仍只显示 12 项，导致用户实际看不到入口的问题。
  - 修复了相关功能页只能提示“去 13 设置”却没有页内跳转入口的问题，避免用户在多层页面间来回找入口。
  - 修复了默认文件夹设置页返回路径固定为首页的问题，现在从功能页跳进去后可以回到对应功能页。
- 更新了什么：
  - 更新了酒馆设置首页说明文案，让默认文件夹设置入口在页头就能看到。
  - 更新了三个依赖 `settings.json` 目标文件夹的修复页交互文案和动作栏，统一提供直达默认文件夹设置的入口。
  - 更新了本次修改的自检结果，`xiaomaojuan.sh`、`lib/tavern_setting_override.sh`、`lib/config.sh` 已通过 `bash -n` 语法检查。

## 2026-03-17 16:33（北京时间 UTC+8）

- 任务：TASK-20260317-09 / 在酒馆设置新增聊天文件备份数量设置
- 改了什么：
  - 在 `lib/tavern_setting_override.sh` 新增 `14 · 聊天文件备份` 设置项、状态显示、详情页和输入处理，接入酒馆设置首页与动作分发。
  - 新增 `backups.common.numberOfBackups` 的读取、状态展示、区块重写和写入逻辑，用于直接修改酒馆 `config.yaml` 里的聊天 / settings 备份留存数量。
  - 将这项写入动作接入酒馆设置统一前置备份链，修改前会先备份对应的 `config.yaml`。
- 修复了什么：
  - 修复了酒馆设置页面里没有“聊天文件备份”入口的问题，避免用户还要去别的流程里找相同能力。
  - 修复了当前项目只能修改小猫卷自己的备份保留数、不能在酒馆设置里直接改 `backups.common.numberOfBackups` 的缺口。
  - 修复了酒馆聊天备份数量修改前缺少前置文件备份保护的问题，现在与其他酒馆设置写入项保持一致。
- 更新了什么：
  - 更新了酒馆设置首页项目总数与说明文案，新增 `14 聊天文件备份` 入口说明。
  - 更新了聊天文件备份页的说明文案，明确它修改的是 `config.yaml` 的 `backups.common.numberOfBackups`，并给出常用建议值区间。
  - 更新了本次修改的自检结果，`xiaomaojuan.sh`、`lib/tavern_setting_override.sh`、`lib/config.sh` 已通过 `bash -n` 语法检查。

## 2026-03-17 16:40（北京时间 UTC+8）

- 任务：TASK-20260317-10 / 修复酒馆设置页未定义颜色变量导致的崩溃
- 改了什么：
  - 将 `lib/tavern_setting_override.sh` 里酒馆设置概览页提示文案使用的未定义颜色变量 `XMJ_LILAC` 替换为主题中实际存在的 `XMJ_LAVENDER`。
  - 保留了默认文件夹入口与聊天备份入口提示行的展示位置和样式，只修正变量名，不改交互结构。
  - 对酒馆设置覆盖文件和主题文件重新执行了语法自检。
- 修复了什么：
  - 修复了进入酒馆设置页时因为 `set -u` 命中未定义变量 `XMJ_LILAC` 而直接中断退出的问题。
  - 修复了酒馆设置首页在显示“默认文件夹入口 / 聊天备份入口”提示行时崩溃、导致后续卡片无法继续渲染的问题。
  - 修复了这类颜色变量拼写残留未被及时发现的问题，本次已确认当前代码中不再残留 `XMJ_LILAC`。
- 更新了什么：
  - 更新了酒馆设置首页提示行的颜色变量，当前统一跟随主题文件中定义的 `XMJ_LAVENDER`。
  - 更新了本次修改的自检结果，`xiaomaojuan.sh`、`lib/tavern_setting_override.sh`、`lib/theme.sh` 已通过 `bash -n` 语法检查。
  - 更新了本地变量扫描结果，`lib/tavern_setting_override.sh` 中已无 `XMJ_LILAC` 残留引用。

## 2026-03-22 12:15（北京时间 UTC+8）

- 任务：TASK-20260322-01 / 阅读并整理 st_manager.sh 的 Termux 保活方案
- 改了什么：
  - 阅读远程项目 `st_manager.sh` 的 `README.md` 与 `angler_toolbox.sh`，定位了保活菜单、唤醒锁、静音音频、电池优化入口与自动启动相关实现。
  - 梳理了项目给出的保活组合策略，区分脚本直接调用的能力与需要用户在系统设置中手动配合完成的部分。
  - 整理了项目中与保活最相关的关键代码段，包括 `termux-wake-lock`、`termux-media-player` 循环播放、`am start` 打开电池优化页，以及通过 `.bashrc`/`.zshrc` 注入自启片段的实现。
- 修复了什么：
  - 修复了当前项目记录链路中缺少这次外部仓库阅读任务持久化记录的问题，确保本次分析可追溯。
  - 修复了对该仓库 Termux 保活方案理解容易停留在 README 概述层的问题，已进一步下钻到脚本函数级别确认具体实现方式。
  - 修复了“保活”和“开机自启”边界容易混淆的问题，已明确区分前台保活、系统设置配合与 Shell 启动注入三类机制。
- 更新了什么：
  - 更新了对 `st_manager.sh` 的整体认知，确认它是一个面向 Android Termux 的单文件工具箱，保活核心是“唤醒锁 + 静音音频 + 电池优化白名单 + 最近任务锁定/通知/自启动设置”。
  - 更新了对其自动化实现的认知，确认所谓“开机自启”主要通过写入 Shell 启动文件触发脚本拉起，而不是依赖 `Termux:Boot` 独立服务。
  - 更新了本次输出依据，后续资料整理将以 `README.md` 的“保活策略详解”和 `angler_toolbox.sh` 中的 `start_silent_audio`、`keep_alive_menu`、`enable_autostart`、`disable_autostart` 等实现为准。

## 2026-03-22 12:53（北京时间 UTC+8）

- 任务：TASK-20260322-02 / 将 Termux 保活设置接入脚本设置中心
- 改了什么：
  - 在 `lib/config.sh` 中新增 `XMJ_KEEPALIVE_AUTO_APPLY_WAKE_LOCK` 与 `XMJ_KEEPALIVE_AUTO_RELEASE_WAKE_LOCK` 默认值、布尔校验与配置校验接入，保证保活开关可持久化且非法值会回退。
  - 在 `lib/render.sh` 与 `lib/router.sh` 中新增“保活设置”入口、详情页与输入分发，支持查看状态、开关自动申请/自动释放，并支持手动立即申请或释放唤醒锁。
  - 在 `lib/launch.sh` 中新增唤醒锁能力检测、运行态状态文件、自动申请/自动释放钩子，并把逻辑接入启动成功、进程结束和停服流程；同时在 `config/xiaomaojuan.conf` 补入保活配置项说明。
- 修复了什么：
  - 修复了当前项目原先只有“是否自启动”但没有真正 Termux 保活配置入口的问题，设置中心现在可以直接管理保活行为。
  - 修复了酒馆启动后与停服后缺少保活生命周期联动的问题，当前可按配置在启动后自动申请唤醒锁、停服后只释放脚本自动申请的唤醒锁。
  - 修复了实际配置文件未显式展示保活开关的问题，当前 `config/xiaomaojuan.conf` 已同步包含保活配置与注释，避免用户只能依赖代码默认值。
- 更新了什么：
  - 更新了脚本设置中心的信息架构，新增“保活设置”页并顺延脚本更新、分支、版本等入口编号。
  - 更新了运行态记录方式，当前使用 `logs/keepalive.state` 记录唤醒锁状态与持有来源，便于页面展示与停止时判断释放条件。
  - 更新了本次自检结果，已使用 Windows 环境下的 Git Bash 对 `xiaomaojuan.sh`、`lib/config.sh`、`lib/render.sh`、`lib/router.sh`、`lib/launch.sh` 与 `config/xiaomaojuan.conf` 执行 `bash -n` 语法检查并通过。

## 2026-03-22 13:44（北京时间 UTC+8）

- 任务：TASK-20260322-03 / 为保活设置补入静音音频循环方案
- 改了什么：
  - 在 `lib/config.sh` 与 `config/xiaomaojuan.conf` 中新增 `XMJ_KEEPALIVE_AUTO_APPLY_AUDIO_LOOP`、`XMJ_KEEPALIVE_AUTO_RELEASE_AUDIO_LOOP` 配置项、默认值与布尔校验，使音频保活开关进入配置中心。
  - 在 `lib/render.sh` 与 `lib/router.sh` 中扩展“保活设置”页，补入音频能力、自动开启/自动停止、运行态展示与 7-12 号操作入口，支持手动开启或停止静音音频循环。
  - 在 `lib/launch.sh` 中新增静音 WAV 本地生成、`termux-media-player` 启停、音频运行态持久化、启动/停服自动联动，以及带唯一标记的守护进程校验逻辑。
- 修复了什么：
  - 修复了当前项目仅有唤醒锁保活、缺少第二道音频保活保障的问题，现已支持唤醒锁与静音音频并行配置。
  - 修复了音频守护进程仅依赖 `kill -0` 判断存活、存在 PID 复用误判风险的问题，当前已通过唯一 marker + 命令行校验后再判活与停止。
  - 修复了静音音频资源依赖外部实现文件的问题，当前改为本地生成 `logs/keepalive-audio/silence-loop.wav`，避免缺失上游脚本时无法接入。
- 更新了什么：
  - 更新了 `logs/keepalive.state` 的结构，新增音频循环 PID 与 marker 持久化字段，便于安全识别守护进程来源。
  - 更新了保活生命周期逻辑，启动后可按配置自动开启静音音频循环，停服后只自动停止脚本自动开启的音频循环。
  - 更新了本轮自检结果，已使用 Windows 环境下的 Git Bash 对 `xiaomaojuan.sh`、`lib/config.sh`、`lib/render.sh`、`lib/router.sh`、`lib/launch.sh` 与 `config/xiaomaojuan.conf` 执行 `bash -n` 语法检查并通过。

## 2026-03-22 14:05（北京时间 UTC+8）

- 任务：TASK-20260322-04 / 优化音频保活缺少依赖时的安装提示
- 改了什么：
  - 在 `lib/render.sh` 的保活设置页中补入音频依赖缺失说明，新增 `Termux:API` 应用仓库、`termux-api-package` 说明页与“安装后返回确认并重启 Termux / 脚本”的引导。
  - 在 `lib/launch.sh` 中调整音频保活的手动开启、启动失败与手动停止提示文案，直接给出 GitHub 链接，方便用户自行选择下载来源。
  - 保留现有音频保活逻辑不变，仅增强缺依赖场景下的可操作提示，避免用户只看到报错却不知道去哪里补齐环境。
- 修复了什么：
  - 修复了音频保活缺少依赖时提示过于笼统的问题，当前会明确告诉用户 `Termux:API` 与 `termux-api-package` 的对应 GitHub 地址。
  - 修复了用户在音频功能不可用时缺少后续动作指引的问题，当前提示已包含“下载完成后回来确认，再重新启动 Termux 或脚本”的步骤。
  - 修复了保活设置页与运行时提醒信息不一致的问题，当前页面内提示与手动操作报错文案已经统一到同一套安装指引。
- 更新了什么：
  - 更新了音频依赖缺失时的交互体验，设置页和通知文案都改成可直接照着处理的安装指引。
  - 更新了本轮自检结果，已使用 Windows 环境下的 Git Bash 对 `lib/render.sh` 与 `lib/launch.sh` 执行 `bash -n` 语法检查并通过。
  - 更新了本项目的变更归档，确保本次提示优化可在 `记录.md` 中按时间追溯。

## 2026-03-22 16:54（北京时间 UTC+8）

- 任务：TASK-20260322-05 / 脚本设置中心接入日志管理
- 改了什么：
  - 在 `lib/config.sh`、`config/xiaomaojuan.conf` 与 `docs/config-guide.md` 中补入日志管理配置，新增 `XMJ_LOG_AUTO_CLEANUP_ON_LAUNCH`，并同步默认值、校验规则、配置示例与逐项说明。
  - 在 `lib/render.sh` 与 `lib/router.sh` 中把原本隐藏的日志页正式挂到脚本设置中心首页，新增“日志管理”入口、启动清理开关页面，以及日志保留数量与启动清理状态展示。
  - 在 `lib/launch.sh` 最后生效的 `xmj_launch_prepare_log_file()` 附近接入启动前旧日志自动整理逻辑，新增状态文本与配置写回函数，启动时会只按保留数量处理旧的 `launch-*.log`。
- 修复了什么：
  - 修复了日志查看、删除、清理能力虽然已存在，但在脚本设置中心首页没有入口、用户无法直接进入的问题。
  - 修复了日志策略只能手动清理、缺少“启动时自动清理旧后台日志”开关的问题，当前可在设置中心直接开关。
  - 修复了启动期日志整理容易误删本次刚创建日志的风险，当前自动清理会跳过本次新建的启动日志，只处理更旧的 `launch-*.log`。
- 更新了什么：
  - 更新了日志管理页的说明文案和交互动作，当前已经支持查看、删除、按保留数量清理，以及配置启动自动清理。
  - 更新了日志配置文档与示例，确保配置文件、默认模板和教程说明保持一致。
  - 更新了本轮自检结果，已使用 Git Bash 对 `xiaomaojuan.sh`、`lib/config.sh`、`lib/render.sh`、`lib/launch.sh`、`lib/router.sh` 执行 `bash -n` 语法检查并通过。
## 2026-03-22 17:14（北京时间 UTC+8）

- 任务：TASK-20260322-06 / main 分支发布 1.0.4 版本
- 改了什么：
  - 在 `main` 分支引入 `test` 分支当前已完成的脚本功能文件，覆盖到启动、设置、日志、保活、酒馆设置、维护与备份相关实现。
  - 重新生成 `CHANGELOG.md` 的 `1.0.4` 版本更新日志，按新增、优化、修复汇总自 `v1.0.3` 以来的完整变更。
  - 更新 `VERSION` 与 `README.md` 的版本信息，使发布文件与本次版本号保持一致。
- 修复了什么：
  - 修复了 `test` 分支改动尚未进入 `main`、正式发布分支与当前可用功能不一致的问题。
  - 修复了 `CHANGELOG.md` 与 `VERSION` 在工作分支中缺失、导致版本展示与更新日志基线中断的问题。
  - 修复了发布说明仍停留在 `1.0.3` 的问题，当前发布文档已同步到 `1.0.4`。
- 更新了什么：
  - 更新了 `main` 分支的发布基线，当前以上一个正式版本 `v1.0.3` 为参照完成了新版本整理。
  - 更新了本轮发布自检结果，已使用 Git Bash 对 `xiaomaojuan.sh`、`lib/config.sh`、`lib/render.sh`、`lib/launch.sh`、`lib/router.sh`、`lib/maintenance_override.sh`、`lib/backup_override.sh`、`lib/tavern_setting_override.sh` 执行 `bash -n` 语法检查并通过。
  - 更新了版本发布资料，后续可直接按 `release: 1.0.4` 提交并推送主分支与版本标签。

## 2026-03-29 23:06（北京时间 UTC+8）

- 任务：TASK-20260329-01 / 首页显示酒馆版本与更新状态
- 改了什么：
  - 在 `lib/launch.sh` 新增首页酒馆版本状态刷新逻辑，读取当前酒馆版本、远端最新版本，并基于本地与远端提交差异生成“是否需要更新”的首页展示文案。
  - 在 `lib/render.sh` 新增首页酒馆版本状态块，并把它插入到“更新维护”分组顶部，使版本信息显示位置处于该分组内、紧邻 `01 启动酒馆` 上方。
  - 在 `lib/render.sh` 的首页渲染入口接入版本状态刷新调用，确保每次回到首页时都会重新计算当前酒馆版本、最新酒馆版本和更新判断结果。
- 修复了什么：
  - 修复了首页原先看不到当前酒馆版本、最新酒馆版本和更新需求状态的问题，避免用户必须进入更新流程后才知道是否需要更新。
  - 修复了用户无法在首页第一时间判断酒馆是否落后远端版本的问题，当前可直接看到“是否需要更新”的明确结论或阻塞原因。
  - 修复了酒馆仓库缺少远端、目录不是 Git 仓库、远端检查失败等场景下首页信息缺少说明的问题，现在会显示对应的无法判断原因。
- 更新了什么：
  - 更新了首页“更新维护”分组的信息结构，在菜单操作项之前增加版本状态摘要区。
  - 更新了首页版本判断逻辑，当前会自动执行远端抓取并按 ahead / behind / latest / diverged 状态输出更具体的结果文案。
  - 更新了本轮自检结果，已使用 Git Bash 对 `xiaomaojuan.sh`、`lib/launch.sh`、`lib/render.sh` 执行 `bash -n` 语法检查并通过。

## 2026-03-29 23:37（北京时间 UTC+8）

- 任务：TASK-20260329-02 / 发布 1.0.5 版本
- 改了什么：
  - 将 `VERSION` 从 `1.0.4` 更新到 `1.0.5`，同步本次发布版本号。
  - 更新 `README.md` 顶部版本说明，使首页文档展示的当前版本与本次发布号一致。
  - 更新 `CHANGELOG.md`，新增 `1.0.5` 条目，记录首页酒馆版本状态展示与更新判断改动。
- 修复了什么：
  - 修复了本次首页版本状态功能完成后，发布版本号仍停留在 `1.0.4` 的问题。
  - 修复了发布说明缺少本轮首页酒馆版本状态改动记录的问题，避免后续查看更新日志时无法追溯本次功能。
  - 修复了版本文件、首页说明与更新日志之间可能出现版本不一致的问题，当前三处内容已统一到 `1.0.5`。
- 更新了什么：
  - 更新了 `main` 分支下一次发布提交的版本资料，当前已整理为 `release: 1.0.5` 对应内容。
  - 更新了版本变更摘要，当前 `1.0.5` 主要收录首页显示酒馆当前版本、最新版本和是否需要更新的能力。
  - 更新了本轮发布归档，确保版本号、更新日志和发布记录都已按北京时间写入项目文件。

## 2026-03-30 00:30（北京时间 UTC+8）

- 任务：TASK-20260330-01 / 处理 test 分支同步 main 时的记录冲突
- 改了什么：
  - 在 `test` 分支处理 `git merge main --no-edit` 产生的 `记录.md` 冲突，手动归并 `main` 分支新增的 `1.0.4` 与 `1.0.5` 发布记录。
  - 保留 `test` 分支既有历史记录，不改动 `lib/launch.sh` 与 `lib/render.sh` 的自动合并结果，只整理记录文件内容。
  - 按当前实际处理时间补入本次冲突解决记录，确保分支同步过程可追溯。
- 修复了什么：
  - 修复了 `记录.md` 在 `main` 与 `test` 合并时出现 add/add 冲突、导致 merge 无法继续完成的问题。
  - 修复了主分支发布记录在测试分支同步链路中可能丢失的问题，当前已保留 `1.0.4` 与 `1.0.5` 两次发布记录。
  - 修复了本次分支同步过程缺少即时持久化记录的问题，当前冲突处理结果已写回项目根目录记录文件。
- 更新了什么：
  - 更新了 `test` 分支当前的记录基线，已同时包含测试分支既有历史与主分支新增发布记录。
  - 更新了本次同步处理阶段状态，当前冲突文件已完成归并，后续只需继续执行 merge 提交与自检。
  - 更新了项目变更归档，确保本次冲突解决符合北京时间分钟级记录规范。

## 2026-04-28 22:02（北京时间 UTC+8）

- 任务：TASK-20260428-01 / 删除音频保活并修复设置后台断开问题
- 改了什么：
  - 在 `lib/config.sh`、`config/xiaomaojuan.conf` 与 `docs/config-guide.md` 中移除了静音音频循环相关配置项，只保留唤醒锁保活配置。
  - 在 `lib/render.sh` 与 `lib/router.sh` 中删除了保活设置页里的音频状态展示、音频说明和 7-12 号音频操作入口，保活页改为仅管理唤醒锁。
  - 在 `lib/launch.sh` 中删除了音频保活的运行时状态、静音音频文件生成、`termux-media-player` 调用、自动启停链路与残留异常片段，收敛为仅保留唤醒锁保活逻辑。
- 修复了什么：
  - 修复了开启设置内音频保活自启动后后台崩掉，并在再次打开设置或启动酒馆后台时突然断开的故障源。
  - 修复了保活设置页仍可进入音频保活分支、导致设置页与启动链路继续触发不稳定音频逻辑的问题。
  - 修复了删除音频功能过程中 `lib/launch.sh` 留下重复函数和残缺代码片段的风险，当前调用链已回到单一唤醒锁路径。
- 更新了什么：
  - 更新了项目当前保活方案，现阶段仅使用唤醒锁，推荐组合调整为“唤醒锁 + 电池不优化 + 最近任务锁定”。
  - 更新了配置与设置中心的功能边界，当前不再提供音频保活的自动开关、手动启停和运行态展示。
  - 更新了本轮自检结果，已通过全局搜索确认业务文件中不再残留音频保活入口或配置引用；命令行 `bash -n` 自检因当前 Windows 环境缺少可用 Bash/WSL 未执行。

## 2026-04-28 22:03（北京时间 UTC+8）

- 任务：TASK-20260428-02 / 发布 1.0.6 版本并推送远端
- 改了什么：
  - 将 `VERSION` 与 `README.md` 中的版本号从 `1.0.5` 更新为 `1.0.6`。
  - 在 `CHANGELOG.md` 中新增 `1.0.6` 条目，记录删除音频保活与修复后台断开的发布内容。
  - 以当前 `test` 分支工作区为基础整理发布文件，准备提交并推送远端仓库。
- 修复了什么：
  - 修复了功能已经改完但版本号仍停留在 `1.0.5`、无法和本轮修复对应的问题。
  - 修复了更新日志缺少本次“删除音频保活”发布记录、后续难以追溯版本变更的问题。
  - 修复了发布资料与当前工作区状态不一致的风险，当前版本文件已与本轮改动同步。
- 更新了什么：
  - 更新了项目发布基线，当前待发布版本为 `1.0.6`。
  - 更新了本轮发布说明，当前版本重点为移除音频保活并恢复后台稳定性。
  - 更新了发布前记录归档，确保版本提升动作已按北京时间写入 `记录.md`。

## 2026-04-28 22:28（北京时间 UTC+8）

- 任务：TASK-20260428-03 / 删除脚本字体管理并改为使用系统默认字体
- 改了什么：
  - 在 `lib/font.sh` 中删除 Termux 字体安装、恢复默认字体和重载设置相关函数，仅保留设置中心共用的 notice 状态函数。
  - 在 `lib/config.sh`、`config/xiaomaojuan.conf`、`docs/config-guide.md` 与 `README.md` 中删除字体预设配置、示例说明和字体管理文档描述，统一改成直接使用 Termux / 手机系统默认字体。
  - 在 `lib/menu_data.sh`、`lib/router.sh` 与 `lib/render.sh` 中删除字体管理入口、页面、路由和编号映射，并同步调整设置中心首页展示与交互提示。
- 修复了什么：
  - 修复了脚本继续暴露字体管理入口、与当前“直接使用系统默认字体”目标相冲突的问题。
  - 修复了字体配置项、文档说明与设置中心实现仍然保留旧字体安装链路、容易让用户误以为还支持脚本管理字体的问题。
  - 修复了字体功能移除后 README 仍残留“字体管理 / `~/.termux/font.ttf`”描述，导致项目说明与实际代码不一致的问题。
- 更新了什么：
  - 更新了设置中心的信息结构，当前仅保留自启动、保活、脚本更新、分支、版本和日志管理入口。
  - 更新了项目默认字体策略，当前完全依赖 Termux / Android 系统自带字体，不再提供内置字体下载、恢复或手动重载能力。
  - 更新了本轮自检基线，后续全局搜索将以“无字体管理残留引用”为完成标准。

## 2026-04-28 22:37（北京时间 UTC+8）

- 任务：TASK-20260428-04 / 发布 1.0.7 版本并准备推送 main
- 改了什么：
  - 将 `VERSION` 与 `README.md` 中的版本号从 `1.0.6` 更新为 `1.0.7`，使本轮字体管理删除改动对应新的发布版本。
  - 在 `CHANGELOG.md` 中新增 `1.0.7` 条目，记录删除脚本字体管理、切回系统默认字体以及相关文档同步清理内容。
  - 同步清理 `README.md` 里仍残留的“内置 Termux 字体预设安装 / 恢复默认字体 / 重新加载设置”旧能力描述，保证发布文档与当前实现一致。
- 修复了什么：
  - 修复了字体功能改完后版本号仍停留在 `1.0.6`、无法区分本轮发布内容的问题。
  - 修复了发布说明缺少“删除字体管理”版本归档，后续查看更新日志无法直接定位这次变更的问题。
  - 修复了 `README.md` 仍保留旧字体能力描述、导致发布文档和实际代码不一致的问题。
- 更新了什么：
  - 更新了项目发布基线，当前待发布版本为 `1.0.7`。
  - 更新了本轮发布重点，当前版本说明已明确改为直接使用 Termux / Android 系统默认字体。
  - 更新了发布前记录归档，确保版本提升与文档同步动作已按北京时间写入 `记录.md`。
