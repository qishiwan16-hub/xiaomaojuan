## 2026-03-17 04:35（北京时间 UTC+8）

- 任务：TASK-20260317-01 / 酒馆设置新增局域网链接
- 改了什么：
  - 在 `xiaomaojuan.sh` 的脚本加载链末尾新增 `lib/tavern_setting_override.sh`，让酒馆设置相关的新增逻辑在 `router.sh` 之后生效。
  - 新增 `lib/tavern_setting_override.sh`，加入局域网 IPv4 识别、局域网链接状态展示、`listen: true` 写入和 `hostWhitelist` 局域网白名单写入逻辑。
  - 扩展酒馆设置总览、详情页和动作分发，新增 `12 · 局域网链接`，支持一键写入、手动修改局域网 IP 和改回自动识别。
- 修复了什么：
  - 修复了酒馆设置里没有局域网访问入口的问题，避免用户还要手动改 `config.yaml` 才能让同一局域网设备访问酒馆。
  - 修复了“安全修复”与局域网访问场景混淆的问题，安全修复页现在明确说明它是本机访问模式，需要局域网访问时应改用新的局域网链接入口。
  - 修复了安全状态提示在局域网白名单场景下容易误判为“未套用本机防护”的问题，状态文案会区分本机访问和局域网白名单。
- 更新了什么：
  - 更新了酒馆设置页的项目数量、说明文案和交互提示，使网络访问相关设置拆分成“安全修复”和“局域网链接”两套清晰流程。
  - 更新了局域网链接详情页展示内容，新增自动识别 IPv4、当前准备写入地址、白名单已写入地址和局域网访问链接展示。
  - 更新了脚本自检结果，本次新增文件和入口文件已通过 `bash -n` 语法检查。

## 2026-03-17 04:45（北京时间 UTC+8）

- 任务：TASK-20260317-02 / 启动到运行阶段判定改为监听行
- 改了什么：
  - 修改 `lib/launch.sh` 的启动判定链，把 `xmj_launch_wait_for_running` 从 `Go to`、入口探测和进程存活兜底，改成只根据后台是否出现 `SillyTavern is listening on ...` 进入运行中。
  - 新增启动日志清洗和监听地址解析逻辑，支持从带终端颜色控制符的后台输出里提取监听地址并反推访问链接。
  - 调整 `lib/render.sh` 的运行页文案，把“等到 Go to 后切进运行页”改成“等到监听成功行后切进运行页”。
- 修复了什么：
  - 修复了启动阶段过早进入“运行中”的问题，避免在只探测到入口可访问或仅凭进程存活时提前切页。
  - 修复了带颜色的原生后台输出可能影响监听地址识别的问题，日志解析时会先去掉 ANSI 控制序列再匹配监听行。
  - 修复了历史启动日志回看时入口链接优先级不准确的问题，现会优先从监听成功行提取访问地址。
- 更新了什么：
  - 更新了运行阶段起始行的回溯判定，兼容新的“监听行判定进入运行阶段”日志标记。
  - 更新了启动超时提示，现在会明确提示后台一直没出现 `SillyTavern is listening on` 这一行。
  - 更新了脚本自检结果，本次修改后的 `xiaomaojuan.sh`、`lib/launch.sh`、`lib/render.sh`、`lib/tavern_setting_override.sh` 已通过 `bash -n` 语法检查。

## 2026-03-17 15:09（北京时间 UTC+8）

- 任务：TASK-20260317-03 / 阅读并梳理 ST-foxium 脚本结构
- 改了什么：
  - 阅读了外部脚本 `C:\Users\祁时晚\Downloads\foxium.sh`，按启动检查、修复功能、优化功能和菜单循环四个层次梳理其实现结构。
  - 提炼了脚本中的主要功能项、对应函数、作用对象和实现手段，准备输出为“内容 -> 如何实现”的对照说明。
  - 补充识别了脚本中的关键实现约束，包括对 `sed/grep/awk` 的文本替换依赖、对特定目录结构和旧版文件位置的假设。
- 修复了什么：
  - 修复了当前项目任务追溯里缺少这次阅读分析记录的问题，确保外部脚本分析任务也有持久化记录。
  - 修复了对目标链接读取路径不完整的阻塞，改为直接读取用户提供的本地 `foxium.sh` 文件本体进行分析。
  - 修复了分析范围不清的问题，已把目标限定为脚本内实际菜单功能与落地实现，而不是泛泛介绍仓库名义功能。
- 更新了什么：
  - 更新了对 ST-foxium 的结构认知，确认它是单文件 Bash 菜单工具，核心通过直接改 ST 文件和运行 git/npm 命令实现功能。
  - 更新了功能清单认知，确认其主菜单为“修复功能”和“优化功能”两组，并带启动检查与自动备份注入能力。
  - 更新了后续输出依据，本次说明将以 `foxium.sh` 中的具体函数和代码段为准。

## 2026-03-17 15:15（北京时间 UTC+8）

- 任务：TASK-20260317-04 / 生成 Foxium 与当前项目功能对照表
- 改了什么：
  - 对照 `foxium.sh` 的菜单项，补充梳理了当前项目中对应的启动、酒馆设置、依赖整理、更新、版本切换、备份、卸载与重装入口。
  - 明确标记了“已有对应实现”“部分对应”“暂无对应”的功能边界，并准备输出为结构化对照表。
  - 补充整理了两边实现方式差异，重点区分了 Foxium 的单文件文本替换方式与当前项目的分模块流程式实现。
- 修复了什么：
  - 修复了仅有单边功能说明、缺少项目内对应落点的问题，现在可以直接看出每个 Foxium 项在当前项目中对应到哪一层实现。
  - 修复了备份、依赖安装和卸载重装这几类跨模块能力不易对齐的问题，已统一映射到当前项目的统一备份流和维护流。
  - 修复了对“可复用 / 不建议照搬”边界不清的问题，输出会明确哪些交互思路可借鉴，哪些硬编码做法不该搬进来。
- 更新了什么：
  - 更新了当前项目能力画像，确认其在备份、更新、版本切换、依赖修复和安全/局域网设置方面已经明显强于 Foxium。
  - 更新了功能缺口画像，确认当前项目仍缺少“强制删除 ST 扩展”“模型列表注入”“Gemini 3 媒体白名单补丁”这类外科式补丁入口。
  - 更新了本次对照说明的依据文件，当前项目主要参考 `lib/launch.sh`、`lib/router.sh`、`lib/tavern_setting_override.sh`、`lib/backup_override.sh`、`lib/maintenance_override.sh`、`lib/update.sh`、`lib/version_switch.sh`。

## 2026-03-17 15:35（北京时间 UTC+8）

- 任务：TASK-20260317-05 / 优化酒馆设置并为写入操作增加前置文件备份
- 改了什么：
  - 在 `lib/tavern_setting_override.sh` 新增酒馆设置专用备份辅助链，统一生成 `backups/tavern-setting-files/时间戳-操作名/` 目录，并按操作复制对应源文件快照。
  - 重写酒馆设置总览页文案，明确提示所有会改文件的设置都会先备份对应文件，再继续执行修改。
  - 将端口修改、安全修复、多用户登录、浏览器跳转、头像高清修复、聊天加载修复、美化卡死修复、文件聊天上限、运行内存、备份保留数、局域网链接写入等设置项全部接入统一前置备份流程。
- 修复了什么：
  - 修复了酒馆设置内多处直接改写 `config.yaml`、`settings.json`、`server-main.js` 或面板配置文件但没有操作前备份的问题。
  - 修复了不同酒馆设置入口在修改前保护措施不一致的问题，避免部分设置可回滚、部分设置不可回滚。
  - 修复了设置执行成功后看不到本次备份落点的问题，状态提示现在会附带备份目录与备份文件摘要。
- 更新了什么：
  - 更新了酒馆设置写入链的交互标准，统一为“先备份对应文件，再执行修改，再返回结果与备份位置”。
  - 更新了酒馆设置页的说明信息，使用户能直接看到这类操作的备份目录位置与保护范围。
  - 更新了本次修改的自检结果，`xiaomaojuan.sh`、`lib/tavern_setting_override.sh`、`lib/router.sh`、`lib/config.sh`、`lib/maintenance.sh` 已通过 `bash -n` 语法检查。

## 2026-03-17 15:43（北京时间 UTC+8）

- 任务：TASK-20260317-06 / 将局域网链接白名单改为局域网网段写入
- 改了什么：
  - 在 `lib/tavern_setting_override.sh` 新增局域网网段换算与匹配逻辑，根据当前私网 IPv4 自动换算 `10.0.0.0/8`、`192.168.0.0/16` 或 `172.16.0.0/12`。
  - 重写局域网链接的白名单生成函数、状态识别函数和详情页展示内容，使局域网链接功能按网段写入 `hostWhitelist`，而不是继续写单个 IPv4。
  - 同步重写局域网链接执行成功文案，让页面显示的“将写入白名单”“已写入白名单”和实际写入到 `config.yaml` 的内容保持一致。
- 修复了什么：
  - 修复了局域网链接功能仍把单个局域网 IP 写进 `hostWhitelist`，与目标教程要求的 `192.168.0.0/16` 这类网段白名单不一致的问题。
  - 修复了局域网链接状态页和安全状态页仍按“单个 IP 白名单”判断的误报问题，避免写入网段后界面显示异常。
  - 修复了局域网 IP 输入页说明与实际行为不一致的问题，现在会明确说明输入的是本机 IPv4，最终写入的是对应局域网网段。
- 更新了什么：
  - 更新了局域网链接详情页说明，明确 `192.168.x.x` 会写成 `192.168.0.0/16`，其他私网段也会自动换算。
  - 更新了局域网链接状态文案，当前状态会带上可访问链接和对应白名单范围。
  - 更新了本次修改的自检结果，`xiaomaojuan.sh` 与 `lib/tavern_setting_override.sh` 已通过 `bash -n` 语法检查。

## 2026-03-17 16:00（北京时间 UTC+8）

- 任务：TASK-20260317-07 / 将酒馆默认文件夹设置提升为独立设置项并持久保存
- 改了什么：
  - 在 `lib/config.sh` 新增 `XMJ_TAVERN_SETTING_USER_NAME` 默认配置项，让酒馆设置里默认使用的 data 用户文件夹名可以持久保存到 `xiaomaojuan.conf`。
  - 在 `lib/tavern_setting_override.sh` 新增 `13 · 默认文件夹设置` 独立入口、状态显示、详情页和写入逻辑，并把写入前备份接入统一酒馆设置备份链。
  - 重写卡顿修复、聊天加载卡死修复、美化卡死修复三页的说明和动作区，移除原来挂在单项功能里的“修改用户名”入口，统一改为引用新的默认文件夹设置。
- 修复了什么：
  - 修复了默认文件夹只存在于单个修复项子流程里的问题，避免用户需要在多个功能页里分别找入口。
  - 修复了默认文件夹只保存在会话变量里、重新进入脚本后又回到 `default-user` 的问题，现在设置后会持续保留。
  - 修复了相关修复项继续使用“用户名”说法导致含义不清的问题，当前页面和提示已统一为“默认文件夹 / data 用户文件夹”。
- 更新了什么：
  - 更新了酒馆设置首页项目数与说明文案，明确需要改 `settings.json` 目标文件夹时统一走 `13 默认文件夹设置`。
  - 更新了相关修复项的交互规则，当前只保留执行入口；修改目标文件夹需要回到酒馆设置首页单独设置。
  - 更新了本次修改的自检结果，`xiaomaojuan.sh`、`lib/config.sh`、`lib/tavern_setting_override.sh` 已通过 `bash -n` 语法检查。

## 2026-03-17 16:26（北京时间 UTC+8）

- 任务：TASK-20260317-08 / 修复默认文件夹设置入口未显示并补充页内跳转
- 改了什么：
  - 修正了当前生效的酒馆设置总览页渲染内容，把 `13 · 默认文件夹设置` 真正显示到卡片区和操作区，并补充了“默认文件夹入口：13”提示行。
  - 在卡顿修复、聊天加载卡死修复、美化卡死修复三页新增 `2 前往默认文件夹设置` 跳转入口。
  - 调整默认文件夹设置页的返回链路，从相关功能页进入时可以回到来源页，而不是一律掉回酒馆设置首页。
- 修复了什么：
  - 修复了酒馆设置动作分发已经支持 `13`，但首页概览页仍只显示 12 项，导致用户实际看不到入口的问题。
  - 修复了相关功能页只能提示“去 13 设置”却没有页内跳转入口的问题，避免用户在多层页面间来回找入口。
  - 修复了默认文件夹设置页返回路径固定为首页的问题，现在从功能页跳进去后可以回到对应功能页。
- 更新了什么：
  - 更新了酒馆设置首页说明文案，让默认文件夹设置入口在页头就能看到。
  - 更新了三个依赖 `settings.json` 目标文件夹的修复页交互文案和动作栏，统一提供直达默认文件夹设置的入口。
  - 更新了本次修改的自检结果，`xiaomaojuan.sh`、`lib/tavern_setting_override.sh`、`lib/config.sh` 已通过 `bash -n` 语法检查。

## 2026-03-17 16:33（北京时间 UTC+8）

- 任务：TASK-20260317-09 / 在酒馆设置新增聊天文件备份数量设置
- 改了什么：
  - 在 `lib/tavern_setting_override.sh` 新增 `14 · 聊天文件备份` 设置项、状态显示、详情页和输入处理，接入酒馆设置首页与动作分发。
  - 新增 `backups.common.numberOfBackups` 的读取、状态展示、区块重写和写入逻辑，用于直接修改酒馆 `config.yaml` 里的聊天 / settings 备份留存数量。
  - 将这项写入动作接入酒馆设置统一前置备份链，修改前会先备份对应的 `config.yaml`。
- 修复了什么：
  - 修复了酒馆设置页面里没有“聊天文件备份”入口的问题，避免用户还要去别的流程里找相同能力。
  - 修复了当前项目只能修改小猫卷自己的备份保留数、不能在酒馆设置里直接改 `backups.common.numberOfBackups` 的缺口。
  - 修复了酒馆聊天备份数量修改前缺少前置文件备份保护的问题，现在与其他酒馆设置写入项保持一致。
- 更新了什么：
  - 更新了酒馆设置首页项目总数与说明文案，新增 `14 聊天文件备份` 入口说明。
  - 更新了聊天文件备份页的说明文案，明确它修改的是 `config.yaml` 的 `backups.common.numberOfBackups`，并给出常用建议值区间。
  - 更新了本次修改的自检结果，`xiaomaojuan.sh`、`lib/tavern_setting_override.sh`、`lib/config.sh` 已通过 `bash -n` 语法检查。

## 2026-03-17 16:40（北京时间 UTC+8）

- 任务：TASK-20260317-10 / 修复酒馆设置页未定义颜色变量导致的崩溃
- 改了什么：
  - 将 `lib/tavern_setting_override.sh` 里酒馆设置概览页提示文案使用的未定义颜色变量 `XMJ_LILAC` 替换为主题中实际存在的 `XMJ_LAVENDER`。
  - 保留了默认文件夹入口与聊天备份入口提示行的展示位置和样式，只修正变量名，不改交互结构。
  - 对酒馆设置覆盖文件和主题文件重新执行了语法自检。
- 修复了什么：
  - 修复了进入酒馆设置页时因为 `set -u` 命中未定义变量 `XMJ_LILAC` 而直接中断退出的问题。
  - 修复了酒馆设置首页在显示“默认文件夹入口 / 聊天备份入口”提示行时崩溃、导致后续卡片无法继续渲染的问题。
  - 修复了这类颜色变量拼写残留未被及时发现的问题，本次已确认当前代码中不再残留 `XMJ_LILAC`。
- 更新了什么：
  - 更新了酒馆设置首页提示行的颜色变量，当前统一跟随主题文件中定义的 `XMJ_LAVENDER`。
  - 更新了本次修改的自检结果，`xiaomaojuan.sh`、`lib/tavern_setting_override.sh`、`lib/theme.sh` 已通过 `bash -n` 语法检查。
  - 更新了本地变量扫描结果，`lib/tavern_setting_override.sh` 中已无 `XMJ_LILAC` 残留引用。
