# 更新日志

## 1.0.6

### 优化

- 移除设置中心中的音频保活配置、菜单入口与运行态展示，保活方案回退为仅保留唤醒锁。
- 收敛后台启动与停服后的保活调用链，避免再次触发 `termux-media-player` 相关不稳定逻辑。

### 修复

- 修复开启音频保活自启动后后台崩掉，并在再次打开设置或启动酒馆后台时突然断开的问题。
- 修复删除音频保活后 `lib/launch.sh` 可能残留重复函数与异常片段、进而影响后续保活链路的问题。

## 1.0.5

### 新增

- 首页“更新维护”分组新增酒馆版本状态摘要，直接显示当前酒馆版本、最新酒馆版本和是否需要更新。

### 优化

- 首页每次渲染时都会自动刷新酒馆远端版本状态，并根据 `ahead` / `behind` / `latest` / `diverged` 输出更具体的判断文案。
- 版本状态摘要固定显示在 `01 启动酒馆` 上方，用户无需进入更新流程就能先判断要不要更新酒馆。

### 修复

- 修复首页原先无法直接判断酒馆是否需要更新的问题。
- 修复酒馆目录缺失、当前目录不是 Git 仓库、远端抓取失败等场景下首页缺少明确提示的问题。

## 1.0.4

### 新增

- 新增酒馆设置覆盖模块，支持局域网链接、默认文件夹设置、聊天文件备份数量，并统一为写入前自动备份目标文件。
- 新增 Termux 保活设置，支持唤醒锁自动申请 / 释放、静音音频循环、运行态展示与手动启停。
- 新增日志管理入口，支持查看、删除、按保留数量清理启动日志，并支持按保留份数在启动前自动清理旧 `launch-*.log`。
- 新增维护与备份覆盖链，补齐更多实际维护 / 备份流程入口。

### 优化

- 启动阶段改为以 `SillyTavern is listening on ...` 作为进入运行态的依据，并增强监听地址解析与启动日志回溯。
- 配置模板、设置中心与文档同步补齐日志目录、日志保留、访问入口、Node 内存、保活开关等说明。
- 音频保活缺少依赖时，直接给出 `Termux:API` 与 `termux-api-package` 的 GitHub 下载指引和重启提示。

### 修复

- 修复局域网白名单写入方式、酒馆设置页未定义颜色变量导致的崩溃、启动后台空白、启动日志处理、分支跳转、更新 / 备份等多处问题。
- 修复日志整理可能误删本次刚创建启动日志的风险，当前自动清理只处理更旧的日志文件。

## 1.0.3

修复酒馆启动加载慢的问题。

## 1.0.2

修复启动错误。

## 1.0.1

修复备份卡顿。

## 1.0.0

下次再更新。
